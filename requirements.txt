# Runtime dependencies, pinned exact so every clone installs the same thing.
#
#   python -m pip install -r requirements.txt
#
# Needs Python 3.10 or newer: failure_log.py uses `str | None` in a signature,
# which raises TypeError at import time on 3.9.
#
# Test-only dependencies (pytest, and httpx for FastAPI's TestClient) are
# deliberately not here — they belong with contributor-runnable tests and CI.
#
# Not covered by pip: a local Ollama instance with `llama3.1` pulled, which
# classifier.py talks to over HTTP. See "Run it" in README.md.

fastapi==0.128.0
pypdf==6.15.0
requests==2.32.4
uvicorn==0.39.0
