# Test-only dependencies, kept out of requirements.txt so the app installs only
# what it runs. CI installs this file; so should a contributor before pytest.
#
#   python -m pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.0.2
httpx==0.28.1  # FastAPI's TestClient needs it
