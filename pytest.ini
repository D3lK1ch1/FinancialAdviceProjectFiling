[pytest]
addopts = -m "not llm"
markers =
    samples: reads the real PDFs in samples/ (gitignored); skipped with a reason when that folder has none
    llm: hits a real, locally running LLM (Ollama); deselected by default, run deliberately with `pytest -m llm`
